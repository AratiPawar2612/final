import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 12,    // Specifies the font size for tokens
    colorPrimary: "#199226",  // Specifies the primary color for tokens
    fontFamily: "Manrope",  
    fontWeightStrong:16 // Specifies the font family for tokens
  },
};

export default theme;

